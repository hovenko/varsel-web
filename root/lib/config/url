[% base = Catalyst.req.base;

   site.url = {
     base    = base
     home    = "${base}"
     message = "${base}message"
   }
   
   site.url.img = {
     link_ext       = "${base}static/gfx/link-ext.gif"
     link_home10    = "${base}static/gfx/link-home10.gif"
     sky            = "${base}static/gfx/sky.gif"
     favicon        = "${base}favicon.ico"
   }
-%]
